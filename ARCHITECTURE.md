# 溯流叩击 — UI 架构文档

## 项目结构
```
溯流叩击/
├── main.py                    # 入口，管理员权限提权
├── vk_map.py                  # 虚拟键码映射表
├── settings.json              # 运行时设置（透明度、主题）
├── presets.json               # 预设配置
├── config/
│   ├── themes.py              # 10套主题色 + Colors类 + 字体定义
│   ├── settings.py            # 设置读写（opacity, theme）
│   └── presets.py             # 预设读写
├── core/
│   ├── keyboard/keyboard_sim.py  # Win32 SendInput 键盘模拟
│   └── mouse/mouse_sim.py        # Win32 SendInput 鼠标模拟
├── tasks/
│   ├── keyboard/keyboard_task.py # 混合任务（键位+点击），支持依赖、循环
│   └── mouse/mouse_task.py       # 纯鼠标任务（当前UI未使用）
└── ui/
    ├── app.py                # 主窗口，模式切换
    ├── titlebar.py           # 自绘标题栏（拖动、按钮）
    ├── keyboard_mode.py      # 主模式：任务卡片、预设、拖拽调整大小
    ├── settings_mode.py      # 设置页：透明度、主题
    ├── mini_mode.py          # 迷你浮动窗口
    ├── layer.py              # 废弃，未使用
    └── mouse_mode.py         # 废弃，未使用
```

## UI 布局

### 主窗口 (overrideredirect=True, 无原生标题栏)
```
┌─────────────────────────────┐ ← 346px 宽
│ ⚡ 工具        [—] [⚙] [✕] │ ← titlebar (place, height=40)
├─────────────────────────────┤
│ [EE ▼]    [删除][保存][加载] │ ← pf 预设栏 (pack top)
│                             │
│  ┌─ 任务卡片1 ────────────┐ │
│  │ [任务名]  ● 就绪        │ │
│  │ ⌨ W [0.5s] ✕          │ │ ← action_frame（有动作时显示）
│  │ ⌨ S [0.5s] ✕          │ │
│  │ [⌨添加键位][🖱添加点击][清空]│
│  │ 循环: [80]s  关系: [独立]│ │
│  │ ● 就绪   [▶ 开始] [✕]  │ │
│  └─────────────────────────┘ │ ← container (pack fill=both, expand=True)
│  ┌─ 任务卡片2 ────────────┐ │
│  │ ...                     │ │
│  └─────────────────────────┘ │
│─────────────────────────────│ ← bf (place, height=66, 固定底部)
│ [＋ 新建任务]  [▶ 全部开始]  │
│═════════════════════════════│ ← handle (place, height=12, 拖拽条)
└─────────────────────────────┘
```

### 最小/最大高度
- 最小: 181px (无任务时)
- 最大: 600px
- 拖拽条在底部，用 Win32 GetCursorPos + SetWindowPos 实现平滑拖拽

### 设置页
```
┌─────────────────────────────┐
│ ⚡ 工具        [—] [⚙] [✕] │
├─────────────────────────────┤
│ 👁 窗口透明度               │
│ [=====slider======] 80%    │
├─────────────────────────────┤
│ 🎨 色彩主题                 │
│ [🔵默认蓝][🟣猫布丁][🔥炭火]│
│ [🌲北欧][🍇葡萄][🌿清新]   │
│ [☀️暖阳][🧊冰川][🌸樱花]   │
│ [⚪极简]                    │
│ ┌─ 预览 ──────────────────┐│
│ │ 主文字 次要文字 弱化文字  ││
│ │ [蓝][绿][红][黄]         ││
│ └─────────────────────────┘│
│ [✓ 应用]                   │
└─────────────────────────────┘
```

### 迷你模式 (CTkToplevel, overrideredirect)
```
┌───────────────────┐ ← 250x100
│ ⚡ 轻松AI      [✕]│ ← 标题栏（可拖动）
│ [取消最小化][▶全部]│ ← 按钮区
│ ● 就绪 / ● 运行中 │ ← 状态（每秒更新）
└───────────────────┘
```

## 功能清单

### 核心功能
1. **混合任务系统** — 一个任务可包含多个键位动作和鼠标点击动作
2. **动作录制** — 按键捕获（GetAsyncKeyState轮询）+ 鼠标点击录制（全屏遮罩）
3. **任务依赖** — "在任务x后"模式，前置任务完成后触发后续任务
4. **循环执行** — 每个任务可设置循环间隔（秒），带倒计时显示
5. **3秒预启动** — 点击开始后3秒倒计时才真正执行
6. **全部开始/停止** — 一键控制所有任务

### UI功能
7. **预设管理** — 保存/加载/删除任务预设（JSON持久化）
8. **自定义标题栏** — 拖动移动窗口，按钮（最小化、设置、关闭）
9. **拖拽调整高度** — 底部拖动条，Win32 API直接操控，DPI补偿
10. **迷你模式** — 浮动小窗口，显示状态，可全部开始/停止
11. **透明度调节** — 30%~100%，实时生效
12. **10套主题** — 暗色5套 + 亮色5套，预览+应用重启
13. **悬浮通知** — 操作反馈（如预设已加载）
14. **任务卡片动态高度** — 根据动作数量自动调整

### 底层能力（非UI，保持不变）
15. **Win32 SendInput** — 键盘/鼠标底层模拟
16. **管理员权限** — 自动提权（ShellExecuteW runas）
17. **进程伪装** — 窗口标题为 "svchost"
18. **安全机制** — 鼠标偏离安全半径自动停止

## 主题系统
- 10套主题定义在 `config/themes.py` 的 `THEMES` 字典
- 每套包含: CARD, ACCENT, BLUE, GREEN, RED, YELLOW, TEXT, TEXT2, DIM, HOVER_GREEN, HOVER_RED
- `Colors` 类通过 `apply(theme_name)` 整体替换
- 字体: MiSans 13px (bold/regular)

## 数据持久化
- `settings.json`: `{"opacity": 0.8, "theme": "🧊 冰川蓝"}`
- `presets.json`: `{"预设名": {"actions": [...], "loop_interval": 80, ...}}`
